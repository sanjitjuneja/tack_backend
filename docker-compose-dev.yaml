version: '3.8'

services:
  web:
    container_name: 'web'
    build: .
    restart: always
    entrypoint: ["/app/docker-entrypoint.sh"]
    volumes:
      - .:/app/
    env_file:
      - tackapp/dev.env
#    environment:
#      - APP=prod
#      - AWS_ACCESS_KEY_ID=AKIAV64RMXYGINNIHWWS
#      - AWS_SECRET_ACCESS_KEY=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
#      - AWS_REGION=us-east-1
#    volumes:
#      - static_volume:/app/tackapp/staticfiles
#      - media_volume:/app/tackapp/media
    command: ["python3", "manage.py", "runserver", "0.0.0.0:8020"]
    ports:
      - "8020:8020"
#    depends_on:
#      - postgres

#  postgres:
#    image: postgres:14.4-alpine
#    container_name: 'postgres'
#    restart: always
#    env_file:
#      - tackapp/dev.env
#    ports:
#      - "5432:5432"

  nginx:
    container_name: 'nginx'
    build: ./nginx
    ports:
      - "80:80"
#    volumes:
#      - static_volume:/app/tackapp/staticfiles
#      - media_volume:/app/tackapp/media
    env_file:
      - tackapp/dev.env
    depends_on:
      - web

  celery:
    build: .
    container_name: 'celery'
    command: celery -A tackapp worker --loglevel=info --logfile=celery.log
#    volumes:
#      - .:/app/
    env_file:
      - tackapp/dev.env
    depends_on:
      - web
      - redis

  redis:
    container_name: 'rediser'
    image: redis:7-alpine
    ports:
      - "6379:6379"

  flower:
    build: .
    container_name: 'flower'
    command: celery -A tackapp --broker=redis://redis:6379/0 flower --port=443
#    ports:
#      - "443:443"
    env_file:
      - tackapp/dev.env
    depends_on:
      - web
      - redis
      - celery
#      - postgres
#
#volumes:
#  static_volume:
#  media_volume: